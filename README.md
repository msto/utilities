# ERISone cluster utilities

* `add_module.py`  
    Create default modulefile template

* `parse_logs.py`  
    Parse LSF log into clean tabular format

* `sub_snake`  
    Submit Snakefile in cluster mode to LSF queues
